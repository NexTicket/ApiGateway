# NexTicket API Gateway Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# Firebase Configuration
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
# Alternative: Use JSON string directly (for production deployment)
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# Backend Services URLs
NOTIFICATION_SERVICE_URL=http://localhost:5000
EVENT_SERVICE_URL=http://localhost:4000
TICKET_SERVICE_URL=http://localhost:8000
PUBLIC_SERVICE_URL=http://localhost:5003

# Security Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:4200
# Comma-separated list of allowed origins for CORS

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
# 15 minutes in milliseconds (15 * 60 * 1000)
RATE_LIMIT_MAX_REQUESTS=100
# Maximum requests per window per IP

# Proxy Configuration
PROXY_TIMEOUT=30000
# Proxy timeout in milliseconds (30 seconds)

# Logging Configuration (optional)
LOG_LEVEL=info
# Options: error, warn, info, debug

# Additional Security (optional)
# JWT_SECRET=your-fallback-jwt-secret
# ENCRYPTION_KEY=your-encryption-key-for-sensitive-data